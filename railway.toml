[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Production stage is now the last stage in Dockerfile, so it is used by default.

[deploy]
startCommand = "PYTHONPATH=/app/src uvicorn src.api:app --host 0.0.0.0 --port $PORT"