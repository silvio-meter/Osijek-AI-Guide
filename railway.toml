[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Production stage is now the last stage in Dockerfile, so it is used by default.

[deploy]
# Railway will use the CMD + ENV defined in the Dockerfile
# (ENV PYTHONPATH=/app/src + CMD uvicorn ... with $PORT support)