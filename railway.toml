[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Production stage is now the last stage in Dockerfile, so it is used by default.

[deploy]
startCommand = ""
# Railway will use the CMD from the Dockerfile (we set target=production in [build])
# The app will automatically listen on $PORT provided by Railway